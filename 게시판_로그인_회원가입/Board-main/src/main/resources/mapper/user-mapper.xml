<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Katsu.Katsu_spring.repository.UserMapper">

    <select id="findById" parameterType="String" resultType="Katsu.Katsu_spring.dto.UserDTO">
        SELECT id, pw FROM member_table WHERE id = #{id}
    </select>

</mapper>
